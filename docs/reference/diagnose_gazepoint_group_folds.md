# `diagnose_gazepoint_group_folds`

**R reference:** gp3ml 0.3.0.

## Diagnose group-aware Gazepoint resampling folds

Creates fold-size, repeat-level, grouping, assessment-coverage, outcome-balance, and exclusion diagnostics for an existing \code{gazepoint_group_folds

## R reference usage

```r
diagnose_gazepoint_group_folds(x, imbalance_review = 1.5, imbalance_fail = 2)
```

The Python implementation is exported as `gp3mlpy.diagnose_gazepoint_group_folds`. See the runtime docstring for Python-specific typing and semantic adaptations.
