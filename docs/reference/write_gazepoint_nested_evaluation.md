# `write_gazepoint_nested_evaluation`

**R reference:** gp3ml 0.3.0.

## Write nested-resampling evaluation tables

Write nested-resampling evaluation tables

## R reference usage

```r
write_gazepoint_nested_evaluation(
  x,
  directory,
  prefix = "gazepoint_nested_evaluation",
  overwrite = FALSE
)
```

The Python implementation is exported as `gp3mlpy.write_gazepoint_nested_evaluation`. See the runtime docstring for Python-specific typing and semantic adaptations.
