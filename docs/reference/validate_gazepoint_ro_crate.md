# `validate_gazepoint_ro_crate`

**R reference:** gp3ml 0.3.0.

## Validate a gp3ml RO-Crate-oriented export

Validate a gp3ml RO-Crate-oriented export

## R reference usage

```r
validate_gazepoint_ro_crate(path)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_ro_crate`. See the runtime docstring for Python-specific typing and semantic adaptations.
