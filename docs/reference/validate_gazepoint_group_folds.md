# `validate_gazepoint_group_folds`

**R reference:** gp3ml 0.3.0.

## Validate group-aware Gazepoint resampling folds

Validate group-aware Gazepoint resampling folds

## R reference usage

```r
validate_gazepoint_group_folds(x)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_group_folds`. See the runtime docstring for Python-specific typing and semantic adaptations.
