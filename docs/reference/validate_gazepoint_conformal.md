# `validate_gazepoint_conformal`

**R reference:** gp3ml 0.3.0.

## Validate a conformal fit

Validate a conformal fit

## R reference usage

```r
validate_gazepoint_conformal(object)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_conformal`. See the runtime docstring for Python-specific typing and semantic adaptations.
