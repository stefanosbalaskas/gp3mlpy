# `audit_gazepoint_nested_resampling`

**R reference:** gp3ml 0.3.0.

## Audit nested grouped resampling for outer-assessment leakage

Audit nested grouped resampling for outer-assessment leakage

## R reference usage

```r
audit_gazepoint_nested_resampling(x)

\method{print}{gp3ml_nested_resampling_audit}(x, ...)
```

The Python implementation is exported as `gp3mlpy.audit_gazepoint_nested_resampling`. See the runtime docstring for Python-specific typing and semantic adaptations.
