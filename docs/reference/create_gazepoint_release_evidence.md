# `create_gazepoint_release_evidence`

**R reference:** gp3ml 0.3.0.

## Create a release evidence manifest

Create a release evidence manifest

## R reference usage

```r
create_gazepoint_release_evidence(
  objects = list(),
  files = character(),
  version = "0.2.0",
  notes = character()
)

\method{print}{gp3ml_release_evidence}(x, ...)
```

The Python implementation is exported as `gp3mlpy.create_gazepoint_release_evidence`. See the runtime docstring for Python-specific typing and semantic adaptations.
