# `validate_gazepoint_nested_evaluation`

**R reference:** gp3ml 0.3.0.

## Validate a nested evaluation

Validate a nested evaluation

## R reference usage

```r
validate_gazepoint_nested_evaluation(x)

\method{print}{gp3ml_nested_evaluation_validation}(x, ...)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_nested_evaluation`. See the runtime docstring for Python-specific typing and semantic adaptations.
