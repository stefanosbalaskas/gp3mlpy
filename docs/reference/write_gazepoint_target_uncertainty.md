# `write_gazepoint_target_uncertainty`

**R reference:** gp3ml 0.3.0.

## Write target-aligned uncertainty tables

Write target-aligned uncertainty tables

## R reference usage

```r
write_gazepoint_target_uncertainty(
  x,
  directory,
  prefix = "gazepoint_target_uncertainty",
  overwrite = FALSE
)
```

The Python implementation is exported as `gp3mlpy.write_gazepoint_target_uncertainty`. See the runtime docstring for Python-specific typing and semantic adaptations.
