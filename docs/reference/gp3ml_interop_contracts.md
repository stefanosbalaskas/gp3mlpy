# `gp3ml_interop_contracts`

**R reference:** gp3ml 0.3.0.

## Cross-package interoperability contracts

Describes a lightweight handoff boundary. Upstream packages remain responsible for their own importing, cleaning, feature derivation, signal processing, sequence processing, and quality control. \code{gp3ml

## R reference usage

```r
gp3ml_interop_contracts()
```

The Python implementation is exported as `gp3mlpy.gp3ml_interop_contracts`. See the runtime docstring for Python-specific typing and semantic adaptations.
