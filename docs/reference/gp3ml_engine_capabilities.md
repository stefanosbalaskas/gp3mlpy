# `gp3ml_engine_capabilities`

**R reference:** gp3ml 0.3.0.

## Audit gp3ml model-engine capabilities

Audit gp3ml model-engine capabilities

## R reference usage

```r
gp3ml_engine_capabilities(check_keras_backend = FALSE)
```

The Python implementation is exported as `gp3mlpy.gp3ml_engine_capabilities`. See the runtime docstring for Python-specific typing and semantic adaptations.
