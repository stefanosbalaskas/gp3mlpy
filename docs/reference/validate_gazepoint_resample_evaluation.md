# `validate_gazepoint_resample_evaluation`

**R reference:** gp3ml 0.3.0.

## Validate a grouped-fold evaluation result

Validate a grouped-fold evaluation result

## R reference usage

```r
validate_gazepoint_resample_evaluation(x)

\method{print}{gp3ml_resample_evaluation_validation}(x, ...)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_resample_evaluation`. See the runtime docstring for Python-specific typing and semantic adaptations.
