# `write_gazepoint_release_checksums`

**R reference:** gp3ml 0.3.0.

## Write SHA-256 checksums for release artifacts

Write SHA-256 checksums for release artifacts

## R reference usage

```r
write_gazepoint_release_checksums(files, path = "SHA256SUMS.csv")
```

The Python implementation is exported as `gp3mlpy.write_gazepoint_release_checksums`. See the runtime docstring for Python-specific typing and semantic adaptations.
