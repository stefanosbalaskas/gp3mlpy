# `audit_gazepoint_group_folds`

**R reference:** gp3ml 0.3.0.

## Aggregate leakage audits across group-aware folds

Aggregate leakage audits across group-aware folds

## R reference usage

```r
audit_gazepoint_group_folds(x)
```

The Python implementation is exported as `gp3mlpy.audit_gazepoint_group_folds`. See the runtime docstring for Python-specific typing and semantic adaptations.
