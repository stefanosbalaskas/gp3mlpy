# `normalize_gazepoint_artifact_text`

**R reference:** gp3ml 0.3.0.

## Normalize volatile text in generated research artifacts

Normalize volatile text in generated research artifacts

## R reference usage

```r
normalize_gazepoint_artifact_text(x, project_path = NULL)
```

The Python implementation is exported as `gp3mlpy.normalize_gazepoint_artifact_text`. See the runtime docstring for Python-specific typing and semantic adaptations.
