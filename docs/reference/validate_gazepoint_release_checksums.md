# `validate_gazepoint_release_checksums`

**R reference:** gp3ml 0.3.0.

## Validate SHA-256 release checksums

Validate SHA-256 release checksums

## R reference usage

```r
validate_gazepoint_release_checksums(manifest, directory = ".")
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_release_checksums`. See the runtime docstring for Python-specific typing and semantic adaptations.
