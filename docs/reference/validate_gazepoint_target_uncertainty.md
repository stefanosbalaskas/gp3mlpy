# `validate_gazepoint_target_uncertainty`

**R reference:** gp3ml 0.3.0.

## Validate target-aligned uncertainty metadata

Validate target-aligned uncertainty metadata

## R reference usage

```r
validate_gazepoint_target_uncertainty(x)

\method{print}{gp3ml_uncertainty_validation}(x, ...)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_target_uncertainty`. See the runtime docstring for Python-specific typing and semantic adaptations.
