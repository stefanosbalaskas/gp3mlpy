# `validate_gazepoint_nested_folds`

**R reference:** gp3ml 0.3.0.

## Validate nested grouped folds

Validate nested grouped folds

## R reference usage

```r
validate_gazepoint_nested_folds(x)

\method{print}{gp3ml_nested_folds_validation}(x, ...)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_nested_folds`. See the runtime docstring for Python-specific typing and semantic adaptations.
