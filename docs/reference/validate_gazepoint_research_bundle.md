# `validate_gazepoint_research_bundle`

**R reference:** gp3ml 0.3.0.

## Validate a synthetic cross-package research bundle

Validate a synthetic cross-package research bundle

## R reference usage

```r
validate_gazepoint_research_bundle(x)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_research_bundle`. See the runtime docstring for Python-specific typing and semantic adaptations.
