# `validate_gazepoint_model_tuning`

**R reference:** gp3ml 0.3.0.

## Validate governed tuning results

Validate governed tuning results

## R reference usage

```r
validate_gazepoint_model_tuning(x)

\method{print}{gp3ml_model_tuning_validation}(x, ...)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_model_tuning`. See the runtime docstring for Python-specific typing and semantic adaptations.
