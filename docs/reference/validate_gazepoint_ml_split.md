# `validate_gazepoint_ml_split`

**R reference:** gp3ml 0.3.0.

## Validate a group-aware Gazepoint holdout split

Validate a group-aware Gazepoint holdout split

## R reference usage

```r
validate_gazepoint_ml_split(x)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_ml_split`. See the runtime docstring for Python-specific typing and semantic adaptations.
