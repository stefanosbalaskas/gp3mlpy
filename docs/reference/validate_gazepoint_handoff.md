# `validate_gazepoint_handoff`

**R reference:** gp3ml 0.3.0.

## Validate a Gazepoint handoff

Validate a Gazepoint handoff

## R reference usage

```r
validate_gazepoint_handoff(x)
```

The Python implementation is exported as `gp3mlpy.validate_gazepoint_handoff`. See the runtime docstring for Python-specific typing and semantic adaptations.
